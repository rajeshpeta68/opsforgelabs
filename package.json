{
  "name": "opsforgelabs",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && sudo systemctl reload nginx"
  },
  "dependencies": {
    "astro": "^5.1.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
